<?xml version="1.0" encoding="UTF-8"?>
<div xmlns:ead="https://archivists.org/ns/ead/v4" xmlns:eac="http://archivists.org/ns/eac/v2" xmlns="http://www.tei-c.org/ns/1.0" type="elementDocumentation" xml:id="elem-formattingExtension">
	<head>
		<gi>formattingExtension</gi>
	</head>
	<div type="fullName">
		<p>Formatting Extension</p>
	</div>
	<div type="summary">
		<p>An optional, wrapping element that may be utilized to encode formatting features that are not part of the EAS data model. When using the EAS-provided NVDL Schema validation procedure, this element will require that all elements encoded within are from the XHTML namespace.</p>
	</div>
	<div type="mayContain">
		<p><list type="gloss">
  <label>[Any element that is in a namespace other than the current namespace]</label>
  <item/>
</list></p>
	</div>
	<div type="mayOccurWithin">
		<!--It seems like we're encoding for display here, so perhaps we should go with an automated Schema-to-TEI approach at some point, rather than keeping these files in sync by hand moving forward
		...especially with EAF, etc. Yikes! -->
		<!-- rather that sort A-Z in one list, would it be better to break out by type?  e.g. narrative appearance vs. findingAidDesc vs. former dsc + new formAvailable ? -->
		<!-- also, in this case, i think we should take a harder line and *only* allow this extension on narrative elements, but also to be more flexible and allow it to sit side-by-side with paragraphs. i can certainly see the benefit of having a formattingExtension follow a local set of "paragraphs". -->
		<p>
				accessConditions, accruals, appraisal, arrangement, biogHist, custodHist, descriptionOfComponents, filePlan, findAidDesc, otherDescription, otherDescriptiveInfo, physicalOrTechnicalRequirements, preferCite, processInfo, publicationNote, recordTypeSpecificStatement, relatedMaterial, scopeContent, separatedMaterial, sourceOfAcquisition, subjectHeadings, useConditions
		</p>
	</div>
	<div type="attributes">
		<p>
			<list type="gloss">
				<label>audience</label>
				<item>Optional</item>
				<label>id</label>
				<item>Optional</item>
				<label>target</label>
				<item>Optional</item>
			</list>
		</p>
	</div>
	<div type="description">
		<p><gi>formattingExtension</gi> provides the option to embed external namespaces directly within the source file, when complex formatting is required. It should preferably be used for embedding XHTML data.</p>
		<p>When using this element, it is recommended that the EAS file is associated with the EAS NVDL for validation purposes.</p>
	</div>
	<div type="availability">
		<p>Within <gi>findAidDesc</gi>: optional, repeatable.</p>
		<p>Within all other elements: optional, not repeatable.</p>
		<p>When used outside of <gi>findAidDesc</gi> to encode a narrative description, the encoder must choose between using a <gi>formattingExtention</gi> element (to wrap XHTML content), or to use one or more <gi>p</gi> elements that are defined within the current EAS schema.</p>
	</div>
</div>
